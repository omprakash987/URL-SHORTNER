
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>url shortner</title>
    <link rel="stylesheet" href="style.css">

</head>
<body>
    <div class="container">

    <h1 class="h1">URL SHORTNER</h1>
   <% if (locals.error) { %>
    
    <p class="error"><%= error %></p>

   <% } %>


    <form class="form" action="/" method="post">
        <input autocomplete="off" class="url" type="url" name="url" placeholder="url here...">
        <input class="button" type="submit" value="Create URL" />
    </form>

    <div class="short-url">
        <% if (locals.short_url) { %>
         <a class="link" target="_blank" href="<%= short_url %>"><%= short_url %></a>
        <% } %>
        
    </div>

</div>

    
</body>
</html>